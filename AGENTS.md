# AGENTS.md

本文件为 AI 协作代理（如 Codex / Copilot 等）在本仓库工作时的约定。审查后可按需调整。

## 项目说明

这是一个 GitHub Profile 仓库（用户名与仓库名相同的特殊仓库 `yongxingzhao/yongxingzhao`）。仓库内的 `README.md` 会自动渲染为 GitHub 个人主页展示内容，是本仓库唯一面向外部的产物。

## 仓库结构

- `README.md`：个人主页展示内容，修改时需遵循 GitHub profile 仓的特殊渲染规则。
- `LICENSE`：GPL-3.0，整体协议，非必要不改动。
- `.git/`：版本控制元数据，勿手动修改。

## 编辑约定

- 默认使用中文沟通；`README.md` 本身内容语言以作者意图为准，改写时保留原意，不擅自翻译或杜撰未确认的信息（如联系方式、技能水平、项目经历等）。
- `README.md` 为 profile 仓库首页，编辑时注意 GitHub Flavored Markdown 的渲染表现：分隔线 `---`、列表、表情符号的显示效果。
- 不要新增未经明确要求的文件、目录、注释或文档。本仓库刻意保持精简。
- 不改任务无关代码或文件；遇到非自己产生的改动，先理解再配合，不擅自回滚。

## 提交约定

- 提交信息使用 Conventional Commits：`docs:` / `chore:` / `fix:` 等前缀加简短描述，例如 `docs: 更新个人主页介绍`。
- 远程为 `git@github.com:yongxingzhao/yongxingzhao.git`，默认分支 `main`。
- 破坏性操作（删除、覆盖、`git reset --hard`、`git push --force`、批量重命名等）前先列出影响范围并确认。

## 依赖与敏感信息

- 本仓库无构建依赖、无包管理器。新增依赖前确认必要性。
- 不硬编码密钥或个人敏感信息（邮箱、手机号、token 等），以占位符或环境变量代替；`README.md` 中的内容由作者本人确认后填写。

## 验证

- 修改 `README.md` 后，在 GitHub 网页端预览渲染效果，或本地用 Markdown 预览工具检查。
- 验证失败时修复根因，不调整内容或护栏强行通过。

## 其他

- LICENSE 为 GPL-3.0，若需更换协议须作者明确同意。
- 个人主页内容（兴趣、技能、联系方式等）属个人表达，代理只做格式与排版调整，内容增删须作者授权。
